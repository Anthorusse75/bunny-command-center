node_modules/
dist/
build/
coverage/
*.log
npm-debug.log*

# Real secrets - never commit
.env
.env.local
.env.*.local
apps/api/.env
apps/web/.env

# Editor/OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Vite
apps/web/dist/
apps/web/.vite/

# Test/coverage artifacts
.nyc_output/
apps/web/test-results/
apps/web/playwright-report/
apps/web/.playwright/
# Playwright storage state (Step 04 auth.setup.ts) - a real, if throwaway/E2E-only,
# session cookie; never committed even though the account behind it is a synthetic
# test-only identity, not a real Discord user.
apps/web/e2e/.auth/
