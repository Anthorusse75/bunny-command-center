// `/notifications` — GLOBAL route. Real content is Step 09's scope.
import { PlaceholderScreen } from "./PlaceholderScreen.js";

export function NotificationsScreen(): React.JSX.Element {
  return (
    <PlaceholderScreen titleKey="notifications.placeholder.title" bodyKey="notifications.placeholder.body" />
  );
}
