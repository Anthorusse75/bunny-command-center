// `/contributions` — GLOBAL route. Real content is Step 18's scope.
import { PlaceholderScreen } from "./PlaceholderScreen.js";

export function ContributionsScreen(): React.JSX.Element {
  return (
    <PlaceholderScreen titleKey="contributions.placeholder.title" bodyKey="contributions.placeholder.body" />
  );
}
